<?php
// libs/shims/mpdf_strict.php
namespace Mpdf;

/**
 * Dočasný stub pre trait Strict.
 * mPDF pôvodne používa tento trait pre interné kontroly.
 * Tento stub minimalizuje fatálne chyby ak trait chýba.
 *
 * Dôležité: Ide len o shim. Ak mPDF volá metódy definované v originálnom trait-e,
 * môže byť potrebné doplniť implementáciu.
 */
trait Strict
{
    // Zatiaľ prázdny stub.
}